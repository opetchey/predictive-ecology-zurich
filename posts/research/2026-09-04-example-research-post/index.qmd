---
title: "Example research post (with executable R)"
description: "A placeholder research post showing how to write a post with an executable R code chunk."
author: "Predictive Ecology Group"
date: 2026-09-04
categories: [research, food webs]
image: cover.jpg
---

*(This is a placeholder post. Replace with real content, or delete once real
posts exist — keep at least this file's structure as a reference for how a
research post is put together.)*

Posts in this `research` folder appear automatically in the feed on the
[Research](/research.qmd) page, because their folder is `posts/research/`
and their `categories:` field starts with `research` (the controlled,
page-mapping tag). Any further tags in `categories:` — here, `food webs` —
are freeform topic tags.

Because this is a `.qmd` file, R code runs when the site is rendered — by
GitHub Actions on every push. For example, a simple logistic population
growth curve, the kind of prediction problem the group works on:

```{r}
#| label: fig-logistic
#| fig-cap: "A simple logistic growth curve — the sort of predictive model the group works with."
#| code-fold: true

r <- 0.3     # intrinsic growth rate
K <- 500     # carrying capacity
N0 <- 10     # initial population size
t <- 0:40

N <- K / (1 + ((K - N0) / N0) * exp(-r * t))

plot(t, N, type = "l", lwd = 2, col = "#0028A5",
     xlab = "Time", ylab = "Population size",
     main = "Logistic growth")
```

Swap this chunk for a real figure, model output, or analysis relevant to
the post.
